"gnome-terminal"
  mod4 + shift + Return

"nautilus /home/`whoami`/Torrents"
  mod4 + shift + t

"nautilus /home/`whoami`/Downloads"
   mod4 + shift + d

# Locks the screen
"gnome-screensaver-command -l"
  mod4 + z

# Takes a screenshot and saves it in the home dir
"scrot"
  Print

# Turn volume up
"soundcontroller up"
  XF86AudioRaiseVolume

# Turn volume down
"soundcontroller down"
  XF86AudioLowerVolume

# Mute/unmute volume
"soundcontroller toggle"
  XF86AudioMute

# Silence the volume
"soundcontroller silence"
  mod4 + XF86AudioLowerVolume

# Sets volume to 100%
"soundcontroller normal"
  mod4 + XF86AudioRaiseVolume

# change sound card
"soundcontroller changesound"
  mod4 + XF86AudioMute

# opens the dmenu launcher
# "dmenu_run -nb '#1a1a1a' -nf '#ffffff' -sb '#aecf96' -sf black -p '>'"
#  mod4 + p

# Google-chrome
"google-chrome"
  mod4 + n

# Google-chrome incognito
"google-chrome --incognito"
  mod4 + shift + n

# Indstiller skærme
"tvconnect"
  XF86Sleep

# Toggles the wireless card
"wireless.sh"
  XF86WLAN

 Power button, should poweroff
"screensaver-command -l; sudo poweroff"
  c:250

# Mod+power button, should go to sleep
"gnome-screensaver-command -l; sudo /etc/acpi/sleep.sh"
  mod4 + c:250

## Mod+shift+power button, should hibernate
#"gnome-screensaver-command -l; sudo /etc/acpi/hibernate.sh"
#  mod4 + shift + c:250

# Changes keyboard layout on Mod+escape
"kblayout"
  mod4 + Escape
